import { Form } from "../../components/Forms"
import { Theme } from "../../components/SideBarTheme"

export const Documentos = () => {
    return (
        <Theme>
         <Form namePage="documentos" nameButton="Novo documento"></Form>
        </Theme>
    )
}